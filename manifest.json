{
	"manifest_version": 2,
	"name": "Disable Page Visibility API",
	"version": "1.0.0",
	"description": "Firefox add-on to disable the Page Visibility API. This prevents e.g. video conferencing systems from tracking whether you are currently in another window.",
	"content_scripts": [
		{
			"matches": ["*://*/*"],
			"js": ["disable.js"]
		}
	]
}
